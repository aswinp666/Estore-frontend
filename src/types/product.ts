export type Product = {
  name: string;
  reviews: number;
  price: number;
  discountedPrice: number;
  _id: number;
  category: string;
  imageUrl: string; // Add this line to store the image URL
};
